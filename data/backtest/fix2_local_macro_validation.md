# Fix #2 — 로컬 바스켓 매크로 프록시 §6/§13 검증 리포트

> 상태: **paper 30영업일 백테스트 검증 완료 · 운영자 승인 대기(live 미반영)**
> 작성: 2026-06-02 (KST) · 검증 실행: 단독 `run_backtest.py` (HTS/traidair 비의존)

## 1. 변경 내용
- 파일: `core/backtest/replay_client.py` — `get_market_data` / 신규 `_basket_macro_proxy`.
- 백테스트 매크로 등급 입력을 **traidair `market-data?mode=sim` → 완전 로컬**로 교체.
  - 지수 분봉(^KS11/^KQ11/^IXIC) 있으면 사용, 없으면(키움 수집=6자리 종목만)
    **유니버스 구성종목(전일 거래대금 상위 40) 바스켓 평균 등락률을 KOSPI 프록시**로
    컷오프(≤ 가상 시각) 산출. 분(hhmm) 단위 캐시.
- 근거: traidair sim 라우트가 `date` 파라미터를 무시하고 **오늘 실시간 매크로**를 반환함을
  실측(20250925 응답 == realtime 응답 바이트 동일) → 과거 일자 재현 불가 = **룩어헤드**.

## 2. 검증 결과 (BACKTEST_DAYS=30, 2023-01-01~2025-12-31 풀에서 랜덤 추첨)
| 항목 | 값 |
|---|---|
| 완주 | 30일 / 목표 30일 (clean exit, 조기종료 0) |
| `localhost:3000` 네트워크 호출(전체 run) | **0회** (이전 ~79회/일) |
| 엔진 속도 | ~10s/일(단독, 페이싱 없음) · 이전 ~22s/일 |
| 시장 등급 분포 | GREEN 2045 / YELLOW 141 / RED 172 / BLACK 12 |
| 총 손익 | +8,450원 (시작 100만 가정) |
| 승일/거래일 | 1/30 (3.3%) |
| 진입/청산 | 6 / 11 |

- **이전 동작**: 등급이 오늘장 기준이라 사실상 always-GREEN. **변경 후**: 그날 실제
  바스켓 움직임으로 RED/BLACK까지 산출 → 룩어헤드 제거 + 네트워크 0.

## 3. 행동 영향 (승인 판단용)
- 등급이 RED/BLACK이면 신규 진입 차단(§2.2.2) → **진입 빈도 감소 방향**. 결과 동일성 아님
  (의도된 정확도 개선이나 전략 결과가 바뀜) → 본 리포트가 §6·§13 승인 대상인 이유.

## 4. funnel 동반 진단 (참고 — 본 변경과 별개의 후속 과제)
30일 32,588회 신호평가 중 **99.9% NO_ENTRY**, ENTRY 22건. 2단 병목:
1. 5분봉 breakout 자체 희소: breakout 통과 4.7%, volume(직전봉 2배) 8.5%.
2. 일봉 게이트/추격금지 오버라이드가 score≥3(1,457회)을 22건으로 추가 차단:
   일봉 RSI 과매수(≥70) · 저항선 근처(고점 -2% 이내) · 3일연속음봉 · 양봉<2 · 종가≤20MA.
- 레버 후보(별도 §6/§13 제안 필요): RSI 과매수컷 70→75±, 저항선 근접 -2%→-0.5%.
  (전부 strategy_params 화이트리스트 / 일봉 게이트 상수 → paper 30영업일 재검증 필수.)

## 5. 승인 체크리스트 (운영자)
- [ ] 룩어헤드 제거(traidair sim 미사용) 타당성 확인
- [ ] 바스켓 프록시를 KOSPI 대용으로 수용
- [ ] 등급 분포(RED/BLACK 발생)로 인한 진입 감소 수용
- [ ] 승인 시: CLAUDE.md §17.2/§21 갱신 반영 확정 + live 신뢰 허용
- [ ] (선택) §18.1 키움 지수 분봉 수집 추가 시 바스켓 프록시 자동 비활성(지수 우선)
