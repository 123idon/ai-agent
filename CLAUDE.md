# ai-agent: 자동주식매매 에이전트 시스템 설계 문서

본 문서는 `C:\ai-team` 프로젝트의 **단일 진실 공급원(SSOT)** 이다.
모든 에이전트, 모듈, 스크립트는 이 문서의 규칙을 어겨선 안 된다.
규칙 변경은 본 파일 수정 → 코드 반영 순서로 진행한다.

---

## 1. 시스템 개요

### 1.1 목적
한국 주식시장(KRX)을 대상으로 한 **다중 에이전트 협업형 자동매매 시스템**.
- KIS Open API(한국투자증권)를 통한 실주문 및 시세 수신
- 모의투자 ↔ 실전투자 두 가지 모드를 명확히 분리
- 학습부를 통한 지속적 전략 개선(단, 실전 운영 중에는 파라미터 잠금)

### 1.2 핵심 원칙
1. **하드리밋 우선**: 어떤 신호·지시·수동개입도 §4의 하드리밋을 넘을 수 없다.
2. **모드 잠금**: 실전 모드 진입 시 전략 파라미터는 읽기 전용(read-only)이 된다.
3. **에이전트 간 메시지 기반 협업**: 직접 함수 호출이 아닌 메시지 큐/이벤트로 협업한다(테스트성·교체용이성).
4. **장애 시 안전한 정지(Fail-Safe)**: 에이전트 1개라도 죽으면 신규 진입은 즉시 중단되고 보유 포지션은 보호 매도 룰을 따른다.
5. **모든 의사결정 기록**: 매수/매도/보류/거절/취소 모든 결정은 사유와 함께 학습부 복기 로그에 남는다.

### 1.3 비범위(Non-Goals)
- 해외주식, 파생상품, 암호화폐는 범위 외(시장상황 에이전트는 KOSPI200 선물·VIX 지표만 참조)
- 옵션·신용·대차 거래 미지원
- HFT(고빈도) 전략 미지원: 최저 신호 주기는 분봉 기준

---

## 2. 조직 구조

```
                          ┌──────────────────┐
                          │   CEO 에이전트    │
                          │ (조율/모드 관리)  │
                          └────────┬─────────┘
              ┌──────────┬─────────┼─────────┬──────────┐
              ▼          ▼         ▼         ▼          ▼
        ┌──────────┐ ┌────────┐ ┌──────┐ ┌──────┐ ┌──────────┐
        │  정보부   │ │ 분석부 │ │리스크│ │실행부│ │  학습부   │
        ├──────────┤ ├────────┤ ├──────┤ ├──────┤ ├──────────┤
        │스크리닝   │ │신호분석│ │리스크│ │주문  │ │복기기록   │
        │시장상황   │ │        │ │관리  │ │실행  │ │패턴/백테 │
        │          │ │        │ │      │ │      │ │실전복기   │
        └──────────┘ └────────┘ └──────┘ └──────┘ └──────────┘
```

### 2.1 CEO 에이전트 (`agents/ceo/`)
- **역할**: 전체 에이전트 라이프사이클 관리, 모드 전환, 비상정지(Kill Switch)
- **입력**:
  - 운영자 명령(`switch_mode`, `kill_switch`, `pause`)
  - 각 부서 헬스체크 결과
  - 일일 손실/연속손절 카운터
- **출력**:
  - 부서별 시작/정지 명령
  - 모드 상태(`paper` / `live`) 브로드캐스트
  - 일일 일지 헤더(개장/폐장 트리거)
- **권한**: 유일하게 §4 §3의 모드 잠금 파일을 수정할 수 있는 주체(다만 하드리밋 yaml은 CEO도 변경 불가)

### 2.2 정보부 (`agents/intel/`)

#### 2.2.1 스크리닝 에이전트 (`agents/intel/screening/`)
- **가동시간**: 장전 08:00 ~ 09:00 (1회 풀스캔 + 09:05 보정)
- **역할**: 당일 매매 후보 종목 선별 → **스크리닝 점수 70점 이상**만 후보로 등록
- **점수 구성(총 100점, 비중은 모의 모드에서 튜닝 가능)**:
  - 전일 거래대금 상위(코스피 500억↑, 코스닥 200억↑): 25점
  - 시가 갭(±1%~+5%): 20점
  - 5일 이동평균 대비 위치 및 추세 정렬: 20점
  - 업종/테마 모멘텀(상위 5개 테마 가산): 15점
  - 공시·뉴스 페널티(악재/관리종목/거래정지 이력): -20점 (필터)
  - 변동성 ATR(20) 적정 범위: 20점
- **출력**: `data/screening/{YYYYMMDD}.json` (후보 리스트 + 점수 + 근거)

#### 2.2.2 시장상황 에이전트 (`agents/intel/market_watch/`)
- **가동시간**: 08:30 ~ 15:35 상시
- **역할**: 매크로 컨디션 감시 → 전체 진입 허용/제한 신호 발행
- **감시 대상**:
  - KOSPI200 선물 가격/베이시스
  - 코스피·코스닥 지수 분봉 추세
  - 환율(USD/KRW), 미국 야간 선물(연동 시)
  - 프로그램 매매 차익/비차익 잔량
- **경보 등급**:
  - `GREEN`: 정상 운영
  - `YELLOW`: 신규 진입 시 신호 4개 충족 강제(조건부 진입 금지)
  - `RED`: 신규 진입 전면 금지, 기존 포지션 보호 매도 룰 가동
  - `BLACK`: 전 시스템 비상정지(CEO에게 Kill 요청)

### 2.3 분석부 — 신호분석 에이전트 (`agents/analysis/signal/`)
- **가동시간**: 09:00 ~ 15:20 (분봉 갱신 주기)
- **역할**: 스크리닝 통과 종목에 대해 **5개 지표** 종합 평가
- **5개 지표**:
  1. **거래량**: 직전 20분 평균 대비 2배 이상 + 양봉 동반
  2. **RSI(14, 1분봉)**: 30~70 사이 + 상승 전환(매수) / 하락 전환(매도)
  3. **MACD(12,26,9)**: 골든크로스(매수) / 데드크로스(매도) + 히스토그램 확장
  4. **이동평균**: 5MA > 20MA > 60MA 정배열(매수) / 역배열(매도)
  5. **캔들 패턴**: 망치형/장대양봉/상승장악형(매수) · 유성형/장대음봉/하락장악형(매도)
- **진입 강도 판정**:
  - 4개 이상 충족 → `STRONG_ENTRY` (강한 진입)
  - 3개 충족 → `CONDITIONAL_ENTRY` (조건부, YELLOW 시 금지)
  - 2개 이하 → `NO_ENTRY` (진입 금지)
- **출력 메시지**: `{symbol, score, indicators, signal, timestamp, reason_text}`

### 2.4 리스크부 — 리스크관리 에이전트 (`agents/risk/risk_manager/`)
- **가동시간**: 08:55 ~ 15:35 상시
- **역할**: §4 하드리밋 절대 감시 + 동적 사이징 + 손익 트래킹
- **검증 항목**(주문 전 게이트):
  - 단일 종목 비중 ≤ 15%
  - 동시 보유 종목 수 ≤ 3
  - 당일 누적 손실 > -2% 여부
  - 연속 손절 카운터 ≥ 3 → 1시간 쿨다운 잠금
  - 진입 시간대 화이트리스트(09:30 ~ 14:29:59)
- **출력**: `APPROVE` / `REJECT(reason)` — 실행부는 `APPROVE` 없이 주문 송신 불가

### 2.5 실행부 — 주문실행 에이전트 (`agents/execution/order/`)
- **가동시간**: 09:00 ~ 15:30
- **역할**: KIS Open API 호출, 모드별 분기
  - `paper` 모드 → KIS 모의투자 도메인(`openapivts.koreainvestment.com:29443`)
  - `live` 모드 → KIS 실전 도메인(`openapi.koreainvestment.com:9443`)
- **주문 종류**:
  - 신규 진입: 분할 매수(50/30/20 또는 단일, 변동성 기반)
  - 익절: §5.3 룰
  - 손절: §5.4 룰
  - 보호 매도: 시장상황 RED 또는 CEO Kill 시 시장가 청산
- **체결 후처리**: 체결 즉시 학습부 `journal`에 raw 체결 푸시

### 2.6 학습부 (`agents/learning/`)

| 하위 에이전트 | 모드 | 역할 |
|---|---|---|
| 복기기록 (`journal/`) | 항상 가동 | 모든 신호·결정·체결·취소를 timestamp 기준 append-only 로그로 저장 |
| 패턴분석/백테스트 (`pattern/`) | **모의 모드 전용** | 누적 로그로 패턴 추출, 파라미터 그리드서치, 백테스트 → 제안서 생성 |
| 실전복기패키징 (`postmortem/`) | **실전 모드 전용** | 일일 단위로 실전 거래를 검토용 패키지(`postmortem_{date}.zip`)로 만들어 운영자에게 제출 |

- **중요**: 패턴분석/백테스트는 실전 모드에서 **실행 자체가 차단**된다(권장 X, 강제 X).
- 실전복기패키징은 통계·차트만 생성하며 **자동 파라미터 변경 권한 없음**.

---

## 3. 모드 시스템

### 3.1 모드 정의
- **paper (모의)**: KIS 모의서버 사용, 가상 자금, 전략 파라미터 수정 가능
- **live (실전)**: KIS 실전서버 사용, 실자금, **모든 전략 파라미터 잠금**

### 3.2 전환 규칙
- 전환 주체: 운영자 → CEO 에이전트 (`scripts/switch_mode.py`)
- 전환 조건:
  - paper → live: 보유 포지션 0 + 미체결 주문 0 + 직전 모의 30영업일 백테스트 통과
  - live → paper: 보유 포지션 0 + 미체결 주문 0 (즉시 가능)
- 전환 시 자동 수행:
  - 모드 잠금 파일 갱신(`config/mode.yaml` → `current_mode` 변경, `locked_until: null` or 다음 영업일)
  - 학습부 하위 에이전트 활성/비활성 토글
  - 전 에이전트 재시작 + 헬스체크

### 3.3 잠금 메커니즘
- `config/strategy_params.yaml`은 live 모드에서 OS 파일 권한 + 애플리케이션 락 이중 보호
- CEO 에이전트는 부팅 시 `mode.yaml.current_mode == "live"`면 `strategy_params.yaml`의 mtime 해시를 메모리에 고정. 운영 중 해시 불일치 감지 시 즉시 Kill Switch 발동.

---

## 4. 하드리밋 (절대 변경 불가)

> 이 절의 모든 수치는 `config/hard_limits.yaml`에 정의되며 **코드/문서/CI 어디서도 수정 PR이 자동 차단**된다. 변경이 필요하면 본 CLAUDE.md를 먼저 수정하고, 운영자 2인 승인 후에만 반영한다.

| ID | 항목 | 값 | 비고 |
|---|---|---|---|
| HL-01 | 1종목 최대 투입 | 총 자산의 15% | 평가금액 기준, 분할매수 합산 |
| HL-02 | 동시 보유 종목 수 | 최대 3종목 | 부분 청산도 1포지션으로 카운트 |
| HL-03 | 일일 손실 한도 | -2% 도달 시 **전면 중단** | 모든 신규 진입 정지, 보유분은 익절/손절 룰만 가동 |
| HL-04 | 연속 손절 쿨다운 | 3연속 손절 시 1시간 신규 진입 금지 | 손절 시각 기준 누적 |
| HL-05 | 장후반 진입 금지 | **14:30 이후 신규 진입 금지** | 14:30:00.000부터 적용, 청산만 허용 |
| HL-06 | 장초반 진입 금지 | **09:00~09:30 신규 진입 금지** | 변동성 확인 윈도우 |
| HL-07 | 슬리피지 가드 | 호가 5틱 이상 슬리피지 예상 시 거절 | 리스크관리 사전검증 |
| HL-08 | 단일 주문 최대 비중 | 종목별 한도(HL-01) 초과 금지 | 분할매수 합산 후에도 동일 |

위 규칙은 우선순위 최상. 신호분석·CEO·운영자 명령도 이를 우회할 수 없다.

---

## 5. 매매 전략 상세

### 5.1 후보 선정 (스크리닝)
- 스크리닝 점수 ≥ 70점만 분석부로 전달
- 점수 < 70점은 분석부에서 자동 거절(`reason: SCREENING_BELOW_THRESHOLD`)

### 5.2 진입 판정
| 지표 충족 수 | 결과 | 비고 |
|---|---|---|
| 4개 이상 | `STRONG_ENTRY` | 분할매수 1차 50% |
| 3개 | `CONDITIONAL_ENTRY` | 시장 GREEN일 때만 진입, 진입 비중 70% 캡 |
| 2개 이하 | `NO_ENTRY` | 진입 금지 |

### 5.3 익절 룰
- **+2%**: 보유 수량의 50% 청산
- **+4%**: 잔여의 30% 청산(=초기 수량의 15%)
- **잔여 35%**: 트레일링 스톱 20%(고점 대비 -20% 시 청산)
- 트레일링은 분봉 종가 기준, 장 마감 15:20까지만 유효 → 15:25 일괄 청산

### 5.4 손절 룰
- **기술적 손절 우선**: 진입 근거(5MA 이탈, MACD 데드크로스, 거래량 소멸 중 2개 이상) 발생 시 즉시 청산
- **하드 손절선**:
  - 스캘핑 모드: -2%
  - 데이트레이딩 모드: -3%
- 기술적/하드 중 먼저 도달한 쪽으로 청산

### 5.5 타임스톱
- 진입 후 **15분 내 방향성 미발생**(±0.3% 박스권) → 재평가, 신호 약화 시 청산
- 진입 후 **30분 내 +0.5% 미달** → 보유 수량 50% 선축소

### 5.6 모드별 전략 캡
- 스캘핑: 평균 보유 5~15분, 손절 -2%, 익절 1차 +1%
- 데이트레이딩(기본): 평균 보유 30분~3시간, 손절 -3%, 익절 1차 +2%
- 모드 선택은 시장상황 GREEN/YELLOW + 종목 변동성(ATR) 기반 자동 결정

---

## 6. 에이전트 간 통신

### 6.1 메시지 버스
- 1차: 로컬 프로세스 → Python `asyncio` + `redis-streams`(개발기), Kafka(향후 확장)
- 토픽 명세:
  - `screening.candidates` (정보부 → 분석부)
  - `market.state` (정보부 → 전 에이전트)
  - `signal.entry`, `signal.exit` (분석부 → 리스크부)
  - `risk.decision` (리스크부 → 실행부)
  - `order.event` (실행부 → 학습부, CEO)
  - `ceo.command` (CEO → 전 에이전트)
  - `learning.proposal` (학습부 → CEO, **paper 전용**)

### 6.2 메시지 표준 스키마(JSON)
```json
{
  "msg_id": "uuid",
  "topic": "string",
  "ts": "ISO-8601",
  "mode": "paper|live",
  "sender": "agent_name",
  "payload": { ... },
  "trace_id": "uuid"
}
```
- 모든 메시지는 `mode` 필드를 포함하며, 수신측은 자신의 모드와 불일치 시 즉시 거절·경보.

---

## 7. 데이터/상태 저장소

| 경로 | 용도 | 보관 |
|---|---|---|
| `data/screening/` | 일자별 스크리닝 결과 JSON | 영구 |
| `data/market/` | 분봉/일봉 캐시(Parquet) | 90일 롤링 |
| `data/journal/` | 복기기록 append-only JSONL | 영구(연단위 압축) |
| `data/backtest/` | 백테스트 산출물 | paper 모드에서만 갱신 |
| `data/postmortem/` | 실전 복기 패키지 | live 모드에서만 갱신 |
| `logs/` | 에이전트별 운영 로그 | 30일 롤링 |
| `state/` | 런타임 상태(포지션, 카운터) | 매일 백업 |

---

## 8. 폴더 구조

```
C:\ai-team\
├── CLAUDE.md                       # 본 설계 문서(SSOT)
├── README.md
├── .gitignore
├── pyproject.toml                  # 의존성·툴 설정
│
├── config/
│   ├── hard_limits.yaml            # §4 하드리밋(변경 금지)
│   ├── strategy_params.yaml        # 전략 파라미터(paper에서만 수정)
│   ├── mode.yaml                   # 현재 모드 + 잠금 상태
│   ├── kis_api.yaml.example        # KIS 키 예시
│   └── logging.yaml
│
├── agents/
│   ├── ceo/                        # CEO 에이전트
│   │   ├── __init__.py
│   │   ├── main.py
│   │   └── README.md
│   ├── intel/
│   │   ├── screening/
│   │   │   ├── main.py
│   │   │   ├── scorer.py
│   │   │   └── README.md
│   │   └── market_watch/
│   │       ├── main.py
│   │       ├── futures.py
│   │       └── README.md
│   ├── analysis/
│   │   └── signal/
│   │       ├── main.py
│   │       ├── indicators.py       # 거래량/RSI/MACD/MA/캔들
│   │       └── README.md
│   ├── risk/
│   │   └── risk_manager/
│   │       ├── main.py
│   │       ├── hard_limits.py      # §4 검증 게이트
│   │       └── README.md
│   ├── execution/
│   │   └── order/
│   │       ├── main.py
│   │       ├── kis_router.py       # paper/live 분기
│   │       └── README.md
│   └── learning/
│       ├── journal/                # 복기기록(항상)
│       │   └── main.py
│       ├── pattern/                # 패턴/백테스트(paper 전용)
│       │   ├── main.py
│       │   └── backtest.py
│       └── postmortem/             # 실전복기 패키징(live 전용)
│           └── main.py
│
├── core/                           # 공통 라이브러리
│   ├── kis_client/                 # KIS Open API SDK 래퍼
│   │   ├── auth.py
│   │   ├── quote.py
│   │   └── order.py
│   ├── indicators/                 # 지표 계산(분석부와 공유)
│   ├── messaging/                  # 메시지 버스 추상화
│   ├── mode_lock/                  # 모드 잠금/해시 검증
│   ├── schemas/                    # Pydantic 메시지 스키마
│   └── time_utils/                 # KRX 영업일/장중 시각 헬퍼
│
├── data/
│   ├── screening/
│   ├── market/
│   ├── journal/
│   ├── backtest/
│   └── postmortem/
│
├── state/                          # 런타임 상태(gitignore)
├── logs/                           # 운영 로그(gitignore)
│
├── scripts/
│   ├── run_paper.py                # 모의투자 기동
│   ├── run_live.py                 # 실전 기동
│   ├── switch_mode.py              # 모드 전환
│   ├── kill_switch.py              # 비상정지
│   └── healthcheck.py
│
└── tests/
    ├── unit/
    ├── integration/
    └── strategy/                   # 전략 회귀 테스트(백테스트 시드 고정)
```

---

## 9. 실행 흐름 (1일 운영 사이클)

| 시각 | 동작 |
|---|---|
| 07:50 | CEO 부팅, 헬스체크, 모드 확인, 모드 잠금 해시 캡처 |
| 08:00 | 스크리닝 시작(전일 마감 데이터 + 야간 뉴스 반영) |
| 08:30 | 시장상황 에이전트 가동, 매크로 등급 산출 |
| 09:00 | 분석부·리스크·실행부 가동 (단, **09:00~09:30 신규 진입 금지** HL-06) |
| 09:30 | 신규 진입 허용 윈도우 시작 |
| 09:30~14:29 | 정상 매매. 분봉 신호 → 리스크 게이트 → 주문 |
| 14:30 | **신규 진입 차단(HL-05)**, 청산/익절/손절만 허용 |
| 15:20 | 트레일링 스톱 종료. 보유분 종가 청산 검토 |
| 15:25 | 잔여 보유분 시장가 청산(스캘핑·데이트레이딩) |
| 15:30 | 정규장 종료, 실행부 종료 |
| 15:35 | 학습부 일일 마감 작업 |
|  | • paper: 패턴분석/백테스트 실행, 제안서 생성 |
|  | • live: 실전복기패키지 빌드 → 운영자 전송 |
| 16:00 | CEO 일일 보고서 생성, 다음 영업일 점검 큐 등록 |

---

## 10. 안전장치 / 비상정지

- **Kill Switch (`scripts/kill_switch.py`)**: 모든 신규 진입 즉시 차단 + 보유 포지션 시장가 청산 옵션
- **데드맨 스위치**: 각 에이전트는 5초 간격 heartbeat 전송, CEO가 15초 이상 미수신 시 해당 부서 격리 + 신규 진입 동결
- **모드 잠금 변조 감지**: `strategy_params.yaml` 해시 불일치 → 즉시 Kill (live 모드)
- **API 인증 실패 3회 연속**: 해당 모드 토큰 폐기 + 재인증 대기
- **체결-주문 불일치**: 미체결/오체결 감지 시 운영자 알림 + 신규 주문 중단

---

## 11. 학습부 운영 원칙

- **복기기록**은 모드와 무관하게 항상 수집(읽기 전용 데이터). 데이터 손실 시 매매 즉시 중단.
- **패턴분석/백테스트**는 paper 모드에서만 동작하며, 산출물은 `learning.proposal` 메시지로 CEO에게 제안. CEO는 운영자 승인 없이 파라미터를 변경하지 못함(자동 적용 금지).
- **실전복기패키징**은 live 모드에서만 동작. 통계·차트·로그를 묶어 `data/postmortem/postmortem_{YYYYMMDD}.zip` 산출. 자동 학습/파라미터 변경 권한 일체 없음.

---

## 12. 기술 스택

- Python 3.11+ (`pyproject.toml`, `uv` or `pip-tools`)
- 비동기: `asyncio`, `aiohttp`
- 데이터: `pandas`, `numpy`, `pyarrow`
- 지표: 자체 구현 우선(`core/indicators/`), 보조로 `ta-lib`/`pandas-ta`
- 메시지 버스: `redis>=7` (Streams)
- 설정 검증: `pydantic v2`
- 시각화(복기): `matplotlib`, `plotly`
- 테스트: `pytest`, `pytest-asyncio`, `hypothesis`
- 린트/포맷: `ruff`, `black`, `mypy --strict`(코어/리스크부 한정)

---

## 13. 개발 가이드라인 (Claude Code 작업 시)

1. **하드리밋 수정 PR 금지**: `config/hard_limits.yaml` 변경 시 본 CLAUDE.md §4도 함께 갱신해야 하며, 두 파일이 동시에 수정되지 않은 PR은 CI에서 거절한다.
2. **실전 코드 경로에 `print` 금지**: 모든 로깅은 `logging.yaml`을 통한 구조화 로그.
3. **메시지 스키마 변경**: `core/schemas/` 갱신 → 모든 에이전트 단위테스트 통과 후 머지.
4. **신규 지표 추가**: 분석부의 5개 지표 비중을 흔드는 변경은 paper 모드에서 30영업일 백테스트 통과 + 운영자 승인 필수.
5. **테스트 우선**: 리스크부와 모드잠금 모듈은 변경 시 반드시 회귀 테스트를 함께 추가.
6. **시간 처리**: 모든 시각은 KST(`Asia/Seoul`) + 영업일 캘린더(`pykrx` 또는 자체 KRX 캘린더)를 사용. naive datetime 사용 금지.

---

## 14. 향후 로드맵 (참고)

- 옵션 헤지 모듈(코스피200 위클리)
- 멀티 계좌 매니저(법인/개인 분리)
- LLM 보조 분석(뉴스/공시 요약, 단 매매 결정권 없음)
- 실시간 대시보드(Grafana)

---

_본 문서가 곧 시스템의 헌법이다. 코드와 문서가 충돌하면 문서 갱신 → 코드 동기화 순으로 처리한다._
